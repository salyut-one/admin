[package]
name = "salyut-admin"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Administrative tooling for salyut.one"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
libc = "0.2"

[profile.release]
lto = "thin"
strip = true
